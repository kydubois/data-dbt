version: 2

sources:
    - name: enterprise_dm
      database: analytics
      schema: enterprise_dm
      tables:
          - name: dim_customers
            description: One record per customer.
          - name: fct_orders
            description: Customer orders